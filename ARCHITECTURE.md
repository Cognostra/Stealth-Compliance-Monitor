# 🛡️ Stealth Compliance Monitor - Architecture Map

## Overview

The **Live-Site Compliance Monitor (LSCM)** is a comprehensive automated testing suite that performs passive security scanning, performance auditing, accessibility testing, and black-box penetration testing on live web applications.

---

## 📊 High-Level Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                                    ENTRY POINT                                       │
│                                   src/index.ts                                       │
│                              (Orchestration Layer)                                   │
└─────────────────────────────────────────┬───────────────────────────────────────────┘
                                          │
          ┌───────────────────────────────┼───────────────────────────────┐
          │                               │                               │
          ▼                               ▼                               ▼
┌─────────────────────┐    ┌─────────────────────────┐    ┌─────────────────────────┐
│   CONFIG LAYER      │    │    CORE RUNNERS         │    │    UTILITIES            │
│   src/config/       │    │    src/core/            │    │    src/utils/           │
├─────────────────────┤    ├─────────────────────────┤    ├─────────────────────────┤
│ • env.ts            │    │ • ComplianceRunner.ts   │    │ • logger.ts             │
│   (EnvConfig)       │    │ • UserFlowRunner.ts     │    │ • throttle.ts           │
│ • compliance.config │    │ • FleetReportGenerator  │    │                         │
└─────────────────────┘    └─────────────────────────┘    └─────────────────────────┘
          │                               │
          │                               │
          ▼                               ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                                  SERVICES LAYER                                      │
│                                 src/services/                                        │
└─────────────────────────────────────────────────────────────────────────────────────┘
          │                               │                               │
          ▼                               ▼                               ▼
┌─────────────────────┐    ┌─────────────────────────┐    ┌─────────────────────────┐
│  AI GENERATION      │    │  HISTORY & TRENDS       │    │  NOTIFICATIONS          │
│  AiRemediation.ts   │    │  HistoryService.ts      │    │  WebhookService.ts      │
├─────────────────────┤    ├─────────────────────────┤    ├─────────────────────────┤
│  PERSISTENCE (WAL)  │    │  BASELINE MANAGEMENT    │    │  SIEM LOGGING           │
│  PersistenceService │    │  BaselineService.ts     │    │  SiemLogger.ts          │
└─────────────────────┘    └─────────────────────────┘    └─────────────────────────┘
```

---

## 🔧 Execution Flow (7 Steps)

```
┌──────────────────────────────────────────────────────────────────────────────────────┐
│  STEP 0: Persistence Initialization (WAL)                                             │
│  ┌──────────────────────────────────────────────────────────────────────────────┐    │
│  │  PersistenceService.init()                                                        │    │
│  │  ├── Create session-specific JSONL log                                        │    │
│  │  └── Enable real-time Write-Ahead Logging for crash resilience                │    │
│  └──────────────────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
┌──────────────────────────────────────────────────────────────────────────────────────┐
│  STEP 1: Browser Initialization (Stealth Mode)                                        │
│  ┌──────────────────────────────────────────────────────────────────────────────┐    │
│  │  BrowserService.initialize()                                                  │    │
│  │  ├── Launch Chromium with stealth args                                         │    │
│  │  ├── Evasion: navigator.webdriver removal                                     │    │
│  │  ├── Signature: Random User-Agent & Viewport                                  │    │
│  │  ├── Configure ZAP Proxy                                                     │    │
│  │  └── Attach Scanners (Network, Secret, A11y, etc.)                            │    │
│  └──────────────────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
┌──────────────────────────────────────────────────────────────────────────────────────┐
│  STEP 2: Authentication                                                               │
│  ┌──────────────────────────────────────────────────────────────────────────────┐    │
│  │  AuthService.login()                                                          │    │
│  │  ├── Navigate to login page                                                  │    │
│  │  ├── Detect authentication method (Supabase/OAuth/Standard)                  │    │
│  │  ├── Fill credentials with human-like delays                                 │    │
│  │  └── Verify successful authentication                                        │    │
│  └──────────────────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
┌──────────────────────────────────────────────────────────────────────────────────────┐
│  STEP 3: Compliance Audit                                                             │
│  ┌──────────────────────────────────────────────────────────────────────────────┐    │
│  │  AuditService.runFullAudit()                                                  │    │
│  │  ├── LighthouseService: Performance, Accessibility, SEO, Best Practices      │    │
│  │  └── ZapService: Passive security alerts from proxy traffic                  │    │
│  └──────────────────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
┌──────────────────────────────────────────────────────────────────────────────────────┐
│  STEP 4: Deep Crawl & Validation                                                      │
│  ┌──────────────────────────────────────────────────────────────────────────────┐    │
│  │  CrawlerService.crawl()                                                       │    │
│  │  ├── Discover high-value links (priority keywords)                           │    │
│  │  ├── Visit up to 15 pages                                                    │    │
│  │  ├── For each page:                                                          │    │
│  │  │   ├── VisualSentinel: Layout regression detection                         │    │
│  │  │   ├── AssetValidator: Check images and media                              │    │
│  │  │   ├── LinkChecker: Verify internal/external links                         │    │
│  │  │   ├── SEOValidator: Meta tags, structure, schema                          │    │
│  │  │   ├── A11yScanner: Accessibility violations (axe-core)                    │    │
│  │  │   ├── InteractionTester: Form and button testing                          │    │
│  │  │   └── ResilienceTester: Error handling checks                             │    │
│  │  └── Collect console errors and network incidents                            │    │
│  └──────────────────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
┌──────────────────────────────────────────────────────────────────────────────────────┐
│  STEP 5: Data Integrity & Logical Testing                                             │
│  ┌──────────────────────────────────────────────────────────────────────────────┐    │
│  │  DataIntegrityService.runIntegrityChecks()                                    │    │
│  │  ├── Count verification (UI vs expected)                                     │    │
│  │  ├── Data consistency checks                                                 │    │
│  │  └── Logical validation rules                                                │    │
│  └──────────────────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
┌──────────────────────────────────────────────────────────────────────────────────────┐
│  STEP 5.5: Supabase & Dependency Scan                                                 │
│  ┌──────────────────────────────────────────────────────────────────────────────┐    │
│  │  SupabaseSecurityScanner.runActiveTests()                                     │    │
│  │  ├── Detect Supabase project URL and keys                                    │    │
│  │  ├── Check for service_role key leaks (CRITICAL)                             │    │
│  │  ├── Test storage bucket listing                                             │    │
│  │  └── Probe REST API for RLS bypass                                           │    │
│  │                                                                               │    │
│  │  FrontendVulnerabilityScanner.scanPageGlobals()                               │    │
│  │  ├── Detect jQuery, Lodash, Angular versions                                 │    │
│  │  ├── Match against known CVE database                                        │    │
│  │  └── Generate upgrade recommendations                                        │    │
│  └──────────────────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
┌──────────────────────────────────────────────────────────────────────────────────────┐
│  STEP 6: Black Box Security Assessment                                                │
│  ┌──────────────────────────────────────────────────────────────────────────────┐    │
│  │  SecurityAssessment.assess()                                                  │    │
│  │  ├── Phase 1: Reconnaissance                                                 │    │
│  │  │   ├── Endpoint discovery                                                  │    │
│  │  │   ├── Tech stack detection                                                │    │
│  │  │   ├── Auth mechanism identification                                       │    │
│  │  │   └── Cookie security analysis                                            │    │
│  │  ├── Phase 2: IDOR Testing (safe ID mutation)                                │    │
│  │  ├── Phase 3: XSS Detection (safe reflection payloads)                       │    │
│  │  ├── Phase 4: SQLi Detection (error/time-based)                              │    │
│  │  ├── Phase 5: Rate Limiting Check                                            │    │
│  │  ├── Phase 6: Authentication Bypass                                          │    │
│  │  ├── Phase 7: Information Disclosure                                         │    │
│  │  └── Data Loss Prevention (DLP)                                              │    │
│  │      └── PiiScanner: Detect SSN, Credit Cards, Phone Numbers (Redaction on)  │    │
│  └──────────────────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
┌──────────────────────────────────────────────────────────────────────────────────────┐
│  STEP 7: Report Generation & Notifications                                            │
│  ┌──────────────────────────────────────────────────────────────────────────────┐    │
│  │  Generate Outputs:                                                            │    │
│  │  ├── {domain}-audit-report.html (Interactive dashboard)                       │    │
│  │  ├── audit-report.pdf          (Executive PDF)                                │    │
│  │  └── report-{timestamp}.json   (Raw findings)                                 │    │
│  │                                                                               │    │
│  │  Dispatch Alerts:                                                             │    │
│  │  ├── WebhookService: Slack/Microsoft Teams/Discord notifications              │    │
│  │  └── SiemLogger: Structured security events to Splunk/Datadog                 │    │
│  └──────────────────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🏗️ Services Dependency Map

```
                              ┌─────────────────┐
                              │  BrowserService │
                              │  (Core Engine)  │
                              └────────┬────────┘
                                       │
        ┌──────────────────────────────┼──────────────────────────────┐
        │                              │                              │
        ▼                              ▼                              ▼
┌───────────────┐            ┌─────────────────┐            ┌─────────────────┐
│   Spies &     │            │    Auth &       │            │    Testing      │
│   Monitors    │            │    Crawling     │            │    Services     │
├───────────────┤            ├─────────────────┤            ├─────────────────┤
│ NetworkSpy    │            │ AuthService     │            │ AuditService    │
│ SecretScanner │            │ CrawlerService  │◄──────────►│ LighthouseServ  │
│ PiiScanner    │            │ DataIntegrity   │            │ ZapService      │
│ SupabaseScan  │            │                 │            │ AiRemediation   │
│ VulnScanner   │            │                 │            │ BaselineService │
└───────────────┘            └─────────────────┘            └─────────────────┘
                                       │
                                       │ Uses
        ┌──────────────────────────────┼──────────────────────────────┐
        │                              │                              │
        ▼                              ▼                              ▼
┌───────────────┐            ┌─────────────────┐            ┌─────────────────┐
│   Page        │            │    Content      │            │    Security     │
│   Validators  │            │    Checkers     │            │    Assessment   │
├───────────────┤            ├─────────────────┤            ├─────────────────┤
│ A11yScanner   │            │ AssetValidator  │            │ SecurityAssess  │
│ SEOValidator  │            │ LinkChecker     │            │   (Pentest)     │
│ VisualSentinel│            │ ResilienceTest  │            │                 │
│ InteractTest  │            │                 │            │                 │
└───────────────┘            └─────────────────┘            └─────────────────┘
                                       │
                                       ▼
                            ┌─────────────────┐
                            │    Report       │
                            │    Generators   │
                            ├─────────────────┤
                            │ ReportGenerator │
                            │ HtmlReportGen   │
                            └─────────────────┘
```

---

## 📁 Directory Structure

```
automated-compliance-runner/
├── src/
│   ├── index.ts                    # Main entry point (orchestrator)
│   │
│   ├── config/
│   │   ├── env.ts                  # Environment configuration & validation
│   │   └── index.ts                # Config exports
│   │
│   ├── core/
│   │   ├── ComplianceRunner.ts     # High-level compliance runner
│   │   ├── UserFlowRunner.ts       # User journey testing
│   │   └── index.ts                # Core exports
│   │
│   ├── services/
│   │   │
│   │   │ ══════════════════════════════════════════════════════════
│   │   │  BROWSER & FOUNDATION
│   │   │ ══════════════════════════════════════════════════════════
│   │   ├── BrowserService.ts       # Playwright wrapper, ZAP proxy, human delays
│   │   ├── AuthService.ts          # Login/authentication handling
│   │   │
│   │   │ ══════════════════════════════════════════════════════════
│   │   │  PASSIVE MONITORING (Attached to BrowserService)
│   │   │ ══════════════════════════════════════════════════════════
│   │   ├── NetworkSpy.ts           # Track network requests/failures
│   │   ├── SecretScanner.ts        # Detect leaked API keys, tokens, credentials
│   │   ├── ConsoleMonitor.ts       # Collect console errors/warnings
│   │   ├── SupabaseSecurityScanner.ts  # Supabase-specific security checks
│   │   ├── FrontendVulnerabilityScanner.ts  # Detect vulnerable JS libraries
│   │   │
│   │   │ ══════════════════════════════════════════════════════════
│   │   │  AUDIT & SCANNING
│   │   │ ══════════════════════════════════════════════════════════
│   │   ├── AuditService.ts         # Orchestrates Lighthouse + ZAP audits
│   │   ├── LighthouseService.ts    # Performance/A11y/SEO scoring
│   │   ├── ZapService.ts           # OWASP ZAP passive security alerts
│   │   │
│   │   │ ══════════════════════════════════════════════════════════
│   │   │  CRAWLING & DISCOVERY
│   │   │ ══════════════════════════════════════════════════════════
│   │   ├── CrawlerService.ts       # Intelligent page discovery & validation
│   │   ├── DataIntegrityService.ts # Logical data verification
│   │   │
│   │   │ ══════════════════════════════════════════════════════════
│   │   │  PAGE VALIDATORS
│   │   │ ══════════════════════════════════════════════════════════
│   │   ├── A11yScanner.ts          # Accessibility testing (axe-core)
│   │   ├── SEOValidator.ts         # SEO meta/structure validation
│   │   ├── VisualSentinel.ts       # Visual regression/layout testing
│   │   ├── AssetValidator.ts       # Image/media validation
│   │   ├── LinkChecker.ts          # Broken link detection
│   │   ├── InteractionTester.ts    # Form/button interaction testing
│   │   ├── ResilienceTester.ts     # Error handling/edge case testing
│   │   │
│   │   │ ══════════════════════════════════════════════════════════
│   │   │  SECURITY (ACTIVE TESTING)
│   │   │ ══════════════════════════════════════════════════════════
│   │   ├── SecurityAssessment.ts   # Black-box pentest (IDOR, XSS, SQLi)
│   │   ├── PiiScanner.ts           # Data Loss Prevention (DLP)
│   │   │
│   │   │ ══════════════════════════════════════════════════════════
│   │   │  INTELLIGENCE & OPERATIONS
│   │   │ ══════════════════════════════════════════════════════════
│   │   ├── AiRemediationService.ts # LLM-based code fix generation
│   │   ├── HistoryService.ts       # Trend analysis & historical tracking
│   │   ├── BaselineService.ts      # False positive management
│   │   │
│   │   │ ══════════════════════════════════════════════════════════
│   │   │  REPORTING & OPERATIONS
│   │   │ ══════════════════════════════════════════════════════════
│   │   ├── WebhookService.ts       # Slack/Teams/Discord universal alerts
│   │   ├── SiemLogger.ts           # Splunk/Datadog structured log forwarder
│   │   ├── PersistenceService.ts   # WAL (Write-Ahead Log) persistence
│   │   ├── ReportGenerator.ts      # Markdown report generator
│   │   ├── HtmlReportGenerator.ts  # Interactive HTML dashboard
│   │   ├── FleetReportGenerator.ts # Multi-site summary generator
│   │   └── index.ts                # Service exports
│   │
│   ├── types/
│   │   └── index.ts                # TypeScript type definitions
│   │
│   └── utils/
│       ├── logger.ts               # Winston logging configuration
│       ├── throttle.ts             # Rate limiting utilities
│       └── index.ts                # Utility exports
│
├── reports/                        # Generated reports output
│   ├── latest.json                 # Most recent JSON report
│   ├── {domain}-audit-report.html  # Interactive HTML dashboard
│   ├── AUDIT_SUMMARY.md            # Markdown summary
│   └── report-{timestamp}.json     # Timestamped JSON archives
│
├── screenshots/                    # Failure screenshots
├── snapshots/                      # Visual regression baselines
├── logs/                           # Application logs
│
├── .env                            # Environment configuration
├── docker-compose.yml              # Container orchestration
├── Dockerfile                      # Docker image definition
├── package.json                    # NPM dependencies
├── tsconfig.json                   # TypeScript configuration
└── playwright.config.ts            # Playwright settings
```

---

## 📊 Data Flow

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                                    DATA FLOW                                         │
└─────────────────────────────────────────────────────────────────────────────────────┘

1. CONFIGURATION (Input)
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │  .env                                                                            │
   │  ├── LIVE_URL         → Target website URL                                      │
   │  ├── AUTH_EMAIL       → Login credentials                                       │
   │  ├── AUTH_PASSWORD    → Login credentials                                       │
   │  ├── ZAP_PROXY_URL    → Security proxy endpoint                                 │
   │  ├── ZAP_API_KEY      → ZAP authentication                                      │
   │  └── MIN/MAX_DELAY_MS → Human-like behavior timing                              │
   └─────────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
2. COLLECTION (Processing)
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │  Browser Session Data Collection                                                 │
   │  ├── Network requests/responses → NetworkSpy                                    │
   │  ├── JavaScript sources        → SecretScanner                                  │
   │  ├── Console messages          → ConsoleMonitor                                 │
   │  ├── Supabase API calls        → SupabaseSecurityScanner                        │
   │  ├── Library globals           → FrontendVulnerabilityScanner                   │
   │  ├── Page content/DOM          → CrawlerService                                 │
   │  ├── Lighthouse metrics        → AuditService                                   │
   │  └── ZAP alerts                → AuditService                                   │
   └─────────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
3. AGGREGATION (Report Object)
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │  Report Object (JSON)                                                            │
   │  {                                                                               │
   │    meta: { version, generatedAt, targetUrl, duration },                         │
   │    authentication: { success, duration },                                       │
   │    crawl: { pagesVisited, pageResults[], failedPages },                         │
   │    integrity: { testsRun, passed, failed, results[] },                          │
   │    network_incidents: NetworkIncident[],                                        │
   │    leaked_secrets: LeakedSecret[],                                              │
   │    supabase_issues: SupabaseSecurityIssue[],                                    │
   │    vulnerable_libraries: VulnerableLibrary[],                                   │
   │    security_assessment: SecurityAssessmentResult,                               │
   │    lighthouse: { scores, metrics },                                             │
   │    security_alerts: SecurityAlert[],                                            │
   │    summary: { scores, alertCounts, passedAudit }                                │
   │  }                                                                               │
   └─────────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
4. OUTPUT (Reports & Alerts)
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │  Generated Artifacts & External Signals                                          │
   │  ├── reports/{domain}-audit-report.html → Visualization dashboard               │
   │  ├── monitoring/webhook             → Instant Slack/Teams payload                │
   │  ├── logs/security-events.log       → SIEM Ingestion (Splunk/Datadog)           │
   │  └── reports/temp/*.jsonl           → Crash recovery data (WAL)                 │
   └─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔌 External Dependencies

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              EXTERNAL INTEGRATIONS                                   │
└─────────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────┐           ┌─────────────────┐           ┌─────────────────┐
│   Playwright    │           │   OWASP ZAP     │           │   Lighthouse    │
│   (Browser)     │           │   (Security)    │           │   (Performance) │
├─────────────────┤           ├─────────────────┤           ├─────────────────┤
│ • Chromium      │           │ • Passive proxy │           │ • Core Web      │
│ • Page control  │           │ • Alert API     │           │   Vitals        │
│ • Network       │           │ • No active     │           │ • A11y scores   │
│   interception  │           │   scanning!     │           │ • SEO scores    │
│ • Screenshots   │           │                 │           │ • Best Practice │
└─────────────────┘           └─────────────────┘           └─────────────────┘
        │                             │                             │
        └─────────────────────────────┼─────────────────────────────┘
                                      │
                                      ▼
                        ┌─────────────────────────┐
                        │     axe-core            │
                        │     (Accessibility)     │
                        ├─────────────────────────┤
                        │ • WCAG 2.1 violations   │
                        │ • Impact severity       │
                        │ • Element targeting     │
                        └─────────────────────────┘
```

---

## 🛡️ Security Testing Capabilities

| Category | Service | Method | Safety Level |
|----------|---------|--------|--------------|
| **Passive Scanning** | ZapService | Proxy traffic analysis | ✅ Read-only |
| **Secret Detection** | SecretScanner | Pattern matching in JS | ✅ Read-only |
| **Supabase Security** | SupabaseSecurityScanner | Key detection + RLS probe | ⚠️ Safe probes |
| **Library Vulns** | FrontendVulnerabilityScanner | Version detection | ✅ Read-only |
| **IDOR Testing** | SecurityAssessment | URL mutation | ⚠️ Safe probes |
| **XSS Detection** | SecurityAssessment | Safe payloads | ⚠️ Non-executing |
| **SQLi Detection** | SecurityAssessment | Error/time-based | ⚠️ Safe probes |
| **Rate Limiting** | SecurityAssessment | 5 rapid requests | ⚠️ Limited volume |
| **Auth Bypass** | SecurityAssessment | Cookie-less access | ⚠️ Safe probes |
| **Info Disclosure** | SecurityAssessment | Common paths check | ✅ Read-only |
| **DLP Scanning** | PiiScanner | Regex (SSN/CC/Phone) | ✅ Read-only |
| **AI Repair** | AiRemediation | GPT-4 Code Fixes | ✅ Passive Gen |
| **Webhook Alerts** | WebhookService | HMAC Signed Payloads | ✅ Notification |
| **SIEM Logging** | SiemLogger | ECS-Compatible Logs | ✅ Observability |
| **Crash Recovery** | PersistenceService | Write-Ahead Logging (WAL) | ✅ Integrity |
| **Stealth Evasion** | BrowserService | UA/Viewport Randomization | ✅ Evasion |

---

## 📈 Output Report Structure

### JSON Report (`latest.json`)

```json
{
  "meta": { "version", "generatedAt", "targetUrl", "duration" },
  "authentication": { "success", "duration" },
  "crawl": { "pagesVisited", "failedPages", "pageResults[]" },
  "integrity": { "testsRun", "passed", "failed" },
  "network_incidents": [],
  "leaked_secrets": [],
  "supabase_issues": [],
  "vulnerable_libraries": [],
  "security_assessment": {
    "findings": [],
    "summary": { "critical", "high", "medium", "low" },
    "reconnaissance": { "endpoints", "techStack", "authMechanism" }
  },
  "lighthouse": { "scores", "metrics" },
  "security_alerts": [],
  "summary": { "scores", "passedAudit" }
}
```

### HTML Dashboard Features

- 🎯 Health Score Gauge (0-100)
- 📊 Score Cards (Performance, Accessibility, SEO, Security)
- ⚡ Quick Wins (High impact, low effort fixes)
- 🔒 Security Assessment Summary
- 📋 Remediation Grid (Sortable, filterable)
- 📈 Historical Trend Charts (Results over time)
- 🤖 AI-Suggested Code Fixes
- 📄 PDF Export Support
- 🔍 Playwright Locators (Copy-to-clipboard)

---

## 🚀 Usage

```bash
# Run full compliance audit
npx ts-node src/index.ts

# With Docker (includes ZAP proxy)
docker-compose up

# View reports
open reports/latest.json
open reports/loadout-audit-report.html
```

---

*Last Updated: 2026-01-15*
